# ⚠️ วิธีรัน Migrations ที่ถูกต้อง

## ❌ ผิด: อย่า Copy ไฟล์ JavaScript!

**อย่า copy ไฟล์ `combine-migrations.js`** ไปวางใน SQL Editor!
- ไฟล์นี้เป็น JavaScript code
- SQL ไม่รู้จัก `//` comment

## ✅ ถูก: Copy ไฟล์ SQL!

**ต้อง copy ไฟล์ `ALL_MIGRATIONS.sql`** เท่านั้น!

### ขั้นตอน:

1. **เปิดไฟล์ `ALL_MIGRATIONS.sql`** ในโปรเจกต์
   - ไฟล์นี้อยู่ใน root directory
   - ไฟล์นี้มี extension `.sql` (ไม่ใช่ `.js`)

2. **Copy เนื้อหาทั้งหมด**
   - กด `Ctrl+A` (เลือกทั้งหมด)
   - กด `Ctrl+C` (copy)

3. **ไปที่ Supabase Dashboard**
   - https://supabase.com/dashboard/project/bwimmqwtmrprnrhdszts/sql

4. **วางใน SQL Editor**
   - กด `Ctrl+V` (paste)
   - ตรวจสอบว่าบรรทัดแรกเป็น `-- ============================================` (ไม่ใช่ `//`)

5. **คลิก Run**

## 🔍 วิธีตรวจสอบว่า Copy ถูกไฟล์:

**ไฟล์ที่ถูกต้อง (`ALL_MIGRATIONS.sql`):**
```
-- ============================================
-- COMBINED MIGRATIONS FOR SUPABASE PROJECT
```

**ไฟล์ที่ผิด (`combine-migrations.js`):**
```
// Combine all migrations into one file for easy execution
import { readdirSync, readFileSync, writeFileSync } from 'fs';
```

## 💡 ถ้ายัง Error:

รัน migrations ทีละไฟล์ตามลำดับใน `RUN_ALL_MIGRATIONS.md`
